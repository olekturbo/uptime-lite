# HTTP server
PORT=8080

# PostgreSQL (matches docker-compose.yml defaults)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/uptime_lite?sslmode=disable

# Redis
REDIS_URL=redis://localhost:6379

# Number of concurrent HTTP-check workers
WORKER_COUNT=10
